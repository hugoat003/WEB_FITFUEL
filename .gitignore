# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.vscode/

# Dependencias y build
node_modules/
dist/
.vite/

# Temporales
*.log
scratchpad/
.wrangler/

# Fotos fuente sin optimizar (no las usa el sitio; se optimizan a public/img/*.webp)
Imagenes productos/

# Herramientas de Claude Code (no son parte del proyecto)
.claude/
.impeccable/

# Documentos de trabajo (catálogos de proveedor, propuestas). Son decenas de MB y, una vez
# comiteados, se quedan en el historial para siempre aunque se borren después.
*.pdf
