User-agent: *
Allow: /

# El panel vive en /admin: Cloudflare redirige /admin.html ahí con un 308, y robots.txt
# compara por prefijo, así que "Disallow: /admin.html" NO cubría /admin — el panel quedaba
# rastreable. Este prefijo cubre las dos formas. Además lleva X-Robots-Tag en _headers, que
# es lo que de verdad impide que se indexe (robots.txt solo impide rastrear).
Disallow: /admin

# /checkout y /cuenta NO se bloquean aquí a propósito. Ya devuelven <meta name="robots"
# content="noindex"> en el HTML, y bloquear el rastreo impediría a Google leer esa etiqueta:
# la URL seguiría pudiendo aparecer en los resultados sin que Google pueda saber que no debe.

Sitemap: https://fitfuelgt.com/sitemap.xml
